import React from 'react';
import { shallow, mount } from 'enzyme';
import sinon from 'sinon';
import { expect } from 'chai';
import InstrumentBubble from './';

const noop = () => {};

describe('Components|BubbleChart/InstrumentBubble', () => {
  describe('when rendered InstrumentBubble', () => {
    it('should render a div with a className of instrumentBubble', () => {
//       const wrapper = shallow(<InstrumentBubble selectedCategory="test" width={1000} height={1500} />);
    });
  });
});
