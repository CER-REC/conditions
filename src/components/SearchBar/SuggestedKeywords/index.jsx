import React from 'react';
import PropTypes from 'prop-types';

class SuggestedKeywords extends React.PureComponent {
  static propTypes = {
  };

  render() {
    return (
      <div className="BubbleChart">
      </div>
    );
  }
}

export default SuggestedKeywords;
