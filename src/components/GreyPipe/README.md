# Component GreyPipe

The GreyPipe component provides a visual connection between the Wheel and either the ProjectMenu or ConditionDetails components.

## Interaction Requirements

This component has no interactions.

## Accessibility Requirements

Should Have a `<title />` for the component Name
Should have a `<description />` explaining the pipe
